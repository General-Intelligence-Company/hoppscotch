#-----------------------Backend Config------------------------------#
# Prisma Config
DATABASE_URL=postgresql://postgres:testpass@hoppscotch-db:5432/hoppscotch

# Sensitive Data Encryption Key while storing in Database (32 character)
# IMPORTANT: Generate a secure random 32-character string for production!
DATA_ENCRYPTION_KEY=data encryption key with 32 char

# Whitelisted origins for the Hoppscotch App.
# This list controls which origins can interact with the app through cross-origin comms.
# - localhost ports (3170, 3000, 3100): app, backend, development servers and services
# - app://localhost_3200: Bundle server origin identifier
#   NOTE: `3200` here refers to the bundle server (port 3200) that provides the bundles,
#   NOT where the app runs. The app itself uses the `app://` protocol with dynamic
#   bundle names like `app://{bundle-name}/`
# For production, replace with your actual domain(s)
WHITELISTED_ORIGINS=http://localhost:3170,http://localhost:3000,http://localhost:3100,app://localhost_3200,app://hoppscotch

# If true, the client's IP address is understood as the left-most entry in the X-Forwarded-For header
# Set to `true` when behind a reverse proxy (recommended for Railway, Heroku, etc.)
TRUST_PROXY=false

#-----------------------Frontend Config------------------------------#

# Base URLs
# For production, replace localhost URLs with your actual domain
VITE_BASE_URL=http://localhost:3000
VITE_SHORTCODE_BASE_URL=http://localhost:3000
VITE_ADMIN_URL=http://localhost:3100

# Backend URLs
# For production, replace localhost URLs with your actual domain
VITE_BACKEND_GQL_URL=http://localhost:3170/graphql
VITE_BACKEND_WS_URL=ws://localhost:3170/graphql
VITE_BACKEND_API_URL=http://localhost:3170/v1

# Terms Of Service And Privacy Policy Links (Optional)
VITE_APP_TOS_LINK=https://docs.hoppscotch.io/support/terms
VITE_APP_PRIVACY_POLICY_LINK=https://docs.hoppscotch.io/support/privacy

# Set default to null as https://proxy.hoppscotch.io/ doesn't require an access token
VITE_PROXYSCOTCH_ACCESS_TOKEN=

#-----------------------Deployment Config------------------------------#

# Set to `true` for subpath based access (all services on single port)
# When true: All services available on port 80 via paths (/admin, /backend, etc.)
# When false: Each service on its own port (3000, 3100, 3170, 3200)
ENABLE_SUBPATH_BASED_ACCESS=false

# Custom port for subpath-based access mode (only used when ENABLE_SUBPATH_BASED_ACCESS=true)
# HOPP_AIO_ALTERNATE_PORT=80

#-----------------------Railway Deployment------------------------------#
# For Railway deployment, use these example production values:
#
# DATABASE_URL=${{Postgres.DATABASE_URL}}  (use Railway's PostgreSQL addon)
# DATA_ENCRYPTION_KEY=<generate-32-char-secure-key>
# WHITELISTED_ORIGINS=https://your-app.railway.app,https://your-app.railway.app:3170,https://your-app.railway.app:3100,app://localhost_3200,app://hoppscotch
# TRUST_PROXY=true
#
# VITE_BASE_URL=https://your-app.railway.app
# VITE_SHORTCODE_BASE_URL=https://your-app.railway.app
# VITE_ADMIN_URL=https://your-app.railway.app:3100
# VITE_BACKEND_GQL_URL=https://your-app.railway.app:3170/graphql
# VITE_BACKEND_WS_URL=wss://your-app.railway.app:3170/graphql
# VITE_BACKEND_API_URL=https://your-app.railway.app:3170/v1
#
# For subpath-based access (recommended for Railway - single port):
# ENABLE_SUBPATH_BASED_ACCESS=true
# VITE_BASE_URL=https://your-app.railway.app
# VITE_ADMIN_URL=https://your-app.railway.app/admin
# VITE_BACKEND_GQL_URL=https://your-app.railway.app/backend/graphql
# VITE_BACKEND_WS_URL=wss://your-app.railway.app/backend/graphql
# VITE_BACKEND_API_URL=https://your-app.railway.app/backend/v1
